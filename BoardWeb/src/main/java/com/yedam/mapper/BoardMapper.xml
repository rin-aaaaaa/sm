<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.yedam.mapper.BoardMapper">

	<select id="selectList" resultType="com.yedam.vo.BoardVO">
		select board_no
		       ,title
		       ,content
		       ,writer
		       ,view_cnt
		       ,writer_date
		from tbl_board
		order by board_no
	</select>
	
	<insert id="insertBoard" parameterType="com.yedam.vo.BoardVO" >
		insert into tbl_board (board_no, title, writer, content)
		values(board_noseq.nextval, #{title}, #{writer}, #{content})
	</insert>

	<update id="updateBoard" parameterType="com.yedam.vo.BoardVO" >
		update tbl_board 
		<set>
		<if test="title != null">
		    title = #{title}
		</if>
		<if test="content != null">
		    ,content = #{content}
		</if>
		</set> 
		where board_no = #{boardNo}
	</update>
	
	<delete id="deleteBoard" parameterType="int">
		delete from tbl_board
		where board_no = #{boardNo}
	</delete>
	
	<select id="selectBoard" parameterType="int">
			select board_no
		       ,title
		       ,content
		       ,writer
		       ,view_cnt
		       ,writer_date
		from tbl_board
		where board_no = #{boardNo}
	</select>
	
</mapper>